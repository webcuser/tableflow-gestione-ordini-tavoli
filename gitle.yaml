{
  "port": 3000,
  "image": "node:22-bookworm",
  "autoPreview": true,
  "setup": "npm --prefix frontend install",
  "build": "npm --prefix frontend run build",
  "run": "npm --prefix frontend run serve"
}
